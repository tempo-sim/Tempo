# Tempo v1 → v2 Migration

v2 is a substantial reorganization of the Tempo plugin layout. The plugins themselves are unchanged; the C++ modules inside them are collapsed and renamed for clarity. This document walks through what changed and what you need to do.

## What changed at a glance

| Plugin | v1 modules | v2 modules |
|---|---|---|
| TempoCore | TempoCore, TempoCoreShared, TempoTime, TempoScripting, TempoCoreEditor | **TempoCore**, TempoCoreEditor |
| TempoSensors | TempoSensors, TempoSensorsShared, TempoCamera, TempoLidar, TempoLabels | **TempoSensors** |
| TempoMovement | TempoMovement, TempoMovementShared, TempoVehicleMovement, TempoVehicleControl | **TempoMovement** |
| TempoAgents | TempoAgents, TempoAgentsShared, TempoAgentsEditor | **TempoAgents**, TempoAgentsEditor |
| TempoWorld | TempoWorld, TempoMapQuery | **TempoWorld** (TempoMapQuery moved to TempoAgents plugin) |
| TempoGeographic, TempoPCG, TempoROS, TempoROSBridge | unchanged | unchanged |

Other notable changes:

- **`ActorControl.proto` → `WorldControl.proto`** in the TempoWorld plugin. The C++ subsystem is renamed `UTempoActorControlServiceSubsystem` → `UTempoWorldControlServiceSubsystem`. The gRPC service is renamed `ActorControlService` → `WorldControlService`.
- **`MapQueries.proto`** moved from the TempoWorld plugin into the TempoAgents plugin (the queries are entirely lane/traffic shaped — they belong with the agents stack).
- **Wire-stable proto packages are NOT preserved.** The proto `package` declarations are now derived from the real (collapsed) module names, so gRPC method paths change for every service whose enclosing module changed. See "External clients" below.
- The leftover `TempoObservableEvents/` directory (untracked artifacts of an abandoned design) was removed. The unused `Greeter` example client was removed from `ExampleClients/`.

## What you need to do

### 1. Pull the new Tempo plugin and rebuild

UE asset references (Blueprints, levels, data assets, save games) to relocated UClasses are handled automatically by `CoreRedirects` shipped in each plugin's `Config/Default<PluginName>.ini` (e.g. `TempoSensors/Config/DefaultTempoSensors.ini`). Open your project — referenced classes resolve to their new modules without needing to re-save.

**Run the settings-section migration script.** UE config files (e.g. your project's `Config/DefaultPlugins.ini`) key class settings by section headers of the form `[/Script/<ModuleName>.<ClassName>]`. CoreRedirects do *not* fix these — UE looks up sections by literal path. Without migration, settings on classes like `UTempoCoreSettings`, `UTempoSensorsSettings`, `UTempoAgentsSettings` (whose owning modules collapsed) silently revert to defaults because their saved values are sitting under the old `[/Script/TempoCoreShared.TempoCoreSettings]` headers that UE no longer looks at. Run:

```sh
python3 Plugins/Tempo/Scripts/migrate_v1_settings_sections.py <YourProjectDir>
```

The script is idempotent — safe to re-run. It walks `<YourProjectDir>/Config/` and rewrites any `/Script/<OldModule>.X` references (section headers and value-side class paths) to the new module name. Run it before reopening the project to avoid the editor saving a fresh defaults-only copy of any settings ini.

The first v2 build also runs an automatic cleanup of the generated Python tree at `Plugins/Tempo/TempoCore/Content/Python/API/tempo/`. Subdirectories that were generated by v1 but no longer have any protos in v2 (e.g., `TempoCamera/`, `TempoLidar/`, `TempoLabels/`, `TempoSensorsShared/`, `TempoMapQuery/`, `TempoVehicleControl/`, `TempoVehicleMovement/`, `TempoMovementShared/`, `TempoAgentsShared/`, `TempoTime/`, `TempoScripting/`, `TempoCoreShared/`) are removed. You'll see a `[Tempo Prebuild] Removed N stale Python output directories: ...` message in the build log.

If you have the override file in place (see #2 below), the override-pinned subdirs will be re-populated and *not* removed — only truly orphaned dirs are cleaned. Custom non-`_pb2` files in any subdir are preserved; the cleanup only removes auto-generated `_pb2.py` / `_pb2_grpc.py` files and empty containers. If you track `Content/Python/API/tempo/` in git, expect a one-time burst of deletions on first v2 build.

### 2. Update your client code

External clients (Python, Rust, C++ wrapper) need to be regenerated against the v2 protos. Two cases:

**Default (clean v2 names everywhere).** Don't add an override file. Regenerate your client. Update your imports to the new module names, e.g.:

```diff
- from tempo.TempoCamera import Camera_pb2
+ from tempo.TempoSensors import Camera_pb2

- from tempo.TempoMapQuery import MapQueries_pb2
+ from tempo.TempoAgents import MapQueries_pb2
```

**Transitional (keep v1 file paths working).** Copy `Plugins/Tempo/Scripts/tempo_package_name_overrides.example.json` to `<YourProject>/Config/tempo_package_name_overrides.json`. With it in place, regenerated Python lands at the v1 paths (`tempo/TempoCamera/Camera_pb2.py`, etc.) and your existing imports keep working without code changes.

The override file controls **file paths only**. The proto `package` declaration — and therefore gRPC method paths and protobuf `Any` type URLs — follows the v2 module layout regardless. As long as you regenerate your client against the new server, both ends agree on the new package names; the wire works. Old, un-regenerated v1 clients will not interop with a v2 server.

When you're ready to fully migrate to v2 names, update your code's imports and remove the override file.

### 3. Service rename: ActorControl → WorldControl

If your client code calls the former `ActorControlService`, you must update the service name. The override mechanism cannot help here — the service name is part of the wire identity:

```diff
- from tempo.TempoWorld import ActorControl_pb2_grpc
- stub = ActorControl_pb2_grpc.ActorControlServiceStub(channel)
+ from tempo.TempoWorld import WorldControl_pb2_grpc
+ stub = WorldControl_pb2_grpc.WorldControlServiceStub(channel)
```

Method names and message types are unchanged.

### 4. Custom UE C++ in your project

If you have a UE C++ module in your own project that `#include`s Tempo headers, update paths to reflect the v2 module layout:

```diff
- #include "TempoCamera/Camera.pb.h"
+ #include "TempoSensors/Camera.pb.h"

- #include "TempoMapQuery/MapQueries.pb.h"
+ #include "TempoAgents/MapQueries.pb.h"

- #include "TempoScripting/Empty.pb.h"
+ #include "TempoCore/Empty.pb.h"
```

Update C++ namespaces correspondingly:

```diff
- TempoCamera::ColorImage image;
+ TempoSensors::ColorImage image;

- TempoScripting::Empty empty;
+ TempoCore::Empty empty;
```

And update Build.cs module dependencies:

```diff
- "TempoCamera",
- "TempoLidar",
- "TempoSensorsShared",
+ "TempoSensors",

- "TempoTime",
- "TempoScripting",
- "TempoCoreShared",
+ "TempoCore",
```

CoreRedirects do not help with C++ source — only assets.

## Proto package overrides reference

The override file (`<YourProject>/Config/tempo_package_name_overrides.json`) maps proto basenames to the virtual module name they should appear as in user-facing artifacts:

```json
{
  "Camera.proto": "TempoCamera"
}
```

Effect:
- `Camera_pb2.py` lands at `python_dest_dir/TempoCamera/Camera_pb2.py` instead of `python_dest_dir/TempoSensors/`.
- `Camera.proto` exports to `Cpp/API/proto/TempoCamera/Camera.proto` instead of `Cpp/API/proto/TempoSensors/`.
- The proto's `package` declaration is **not** changed.

Validation runs at proto-generation time:
- A warning prints if an override key matches no current proto file (probably a typo or stale entry).
- A hard error fires if two protos with overrides claim the same virtual module name from different real modules.

## Rationale for not preserving the wire

We considered shipping explicit `package <v1-name>;` declarations in every moved proto so v1 clients would keep working at the gRPC level. We chose not to, because the v1 names would then have to live in Tempo source forever, which defeats the cleanup. Wire stability is broken once at v2; new clients (and migrated old ones) work cleanly thereafter.
