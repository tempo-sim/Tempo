# Full dependency set for the Tempo build venv (TempoEnv). This is the authoritative
# list for what GenAPI.sh installs into the venv, and it must stay broader than the
# published tempo-sim package: it covers the API runtime deps AND the build-time
# generators (Jinja2) AND the heavy example-client / visualization deps.
#
# pyproject.toml declares a different, deliberately trimmed dependency contract for
# external `pip install tempo-sim` consumers (core runtime in [project].dependencies,
# the heavy deps in the [examples] extra). Keep the two in sync when versions change:
# this file = what the local venv gets; pyproject.toml = what published installs get.

# API runtime (mirrors pyproject [project].dependencies)
protobuf==6.33.6
grpcio==1.62.2
curio-compat==1.6.7
prompt_toolkit>=3.0

# Build-time only (the code generators); not a published runtime dep
Jinja2==3.1.6

# Build-time only: lets Scripts/Package.sh build the generated client's sdist+wheel into the
# Packaged/ artifact (Packaged/API/Python). Without it, Package.sh falls back to a wheel-only
# build via pip.
build==1.2.2.post1

# Example clients / visualization (mirrors pyproject [examples] extra)
opencv-python-headless==4.10.0.84
matplotlib==3.9.2
pynput==1.7.7
pyvista==0.46.1
pyvistaqt==0.11.3
qasync==0.27.1
PyQt5==5.15.11
av==17.0.1
# RerunPlayground example client: rerun viewer/logging + Gradio control panel.
rerun-sdk==0.26.2
gradio==6.26.0
# Gradio's own web stack. Gradio constrains these loosely (fastapi>=0.115.2,<1.0 and
# starlette>=1.0.1,<2.0); they are pinned here so the venv resolves reproducibly.
fastapi==0.141.1
starlette==1.6.0
