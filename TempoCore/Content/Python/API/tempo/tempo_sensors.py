# Warning: Autogenerated code do not edit

from tempo._tempo_context import tempo_context
from tempo import run_async
from curio.meta import awaitable
import asyncio


import TempoSensors.Labels_pb2, TempoSensors.Labels_pb2_grpc, TempoCore.Empty_pb2
async def _get_instance_to_semantic_id_map(
) -> TempoSensors.Labels_pb2.InstanceToSemanticIdMap:
    stub = await tempo_context().get_stub(TempoSensors.Labels_pb2_grpc.LabelServiceStub)
    request = TempoCore.Empty_pb2.Empty(
    )
    return await stub.GetInstanceToSemanticIdMap(request)


def get_instance_to_semantic_id_map(
) -> TempoSensors.Labels_pb2.InstanceToSemanticIdMap:
    return run_async(_get_instance_to_semantic_id_map(
    ))


@awaitable(get_instance_to_semantic_id_map)
async def get_instance_to_semantic_id_map(
) -> TempoSensors.Labels_pb2.InstanceToSemanticIdMap:
    return await _get_instance_to_semantic_id_map(
    )

import TempoSensors.Labels_pb2, TempoSensors.Labels_pb2_grpc, TempoCore.Empty_pb2
async def _get_all_actor_labels(
) -> TempoSensors.Labels_pb2.GetAllActorLabelsResponse:
    stub = await tempo_context().get_stub(TempoSensors.Labels_pb2_grpc.LabelServiceStub)
    request = TempoCore.Empty_pb2.Empty(
    )
    return await stub.GetAllActorLabels(request)


def get_all_actor_labels(
) -> TempoSensors.Labels_pb2.GetAllActorLabelsResponse:
    return run_async(_get_all_actor_labels(
    ))


@awaitable(get_all_actor_labels)
async def get_all_actor_labels(
) -> TempoSensors.Labels_pb2.GetAllActorLabelsResponse:
    return await _get_all_actor_labels(
    )

import TempoSensors.Labels_pb2, TempoSensors.Labels_pb2_grpc, TempoCore.Empty_pb2
async def _get_labeled_actor_types(
) -> TempoSensors.Labels_pb2.GetLabeledActorTypesResponse:
    stub = await tempo_context().get_stub(TempoSensors.Labels_pb2_grpc.LabelServiceStub)
    request = TempoCore.Empty_pb2.Empty(
    )
    return await stub.GetLabeledActorTypes(request)


def get_labeled_actor_types(
) -> TempoSensors.Labels_pb2.GetLabeledActorTypesResponse:
    return run_async(_get_labeled_actor_types(
    ))


@awaitable(get_labeled_actor_types)
async def get_labeled_actor_types(
) -> TempoSensors.Labels_pb2.GetLabeledActorTypesResponse:
    return await _get_labeled_actor_types(
    )

import TempoSensors.Labels_pb2, TempoSensors.Labels_pb2_grpc, TempoCore.Empty_pb2
async def _get_semantic_classes(
) -> TempoSensors.Labels_pb2.GetSemanticClassesResponse:
    stub = await tempo_context().get_stub(TempoSensors.Labels_pb2_grpc.LabelServiceStub)
    request = TempoCore.Empty_pb2.Empty(
    )
    return await stub.GetSemanticClasses(request)


def get_semantic_classes(
) -> TempoSensors.Labels_pb2.GetSemanticClassesResponse:
    return run_async(_get_semantic_classes(
    ))


@awaitable(get_semantic_classes)
async def get_semantic_classes(
) -> TempoSensors.Labels_pb2.GetSemanticClassesResponse:
    return await _get_semantic_classes(
    )

import TempoSensors.Labels_pb2, TempoSensors.Labels_pb2_grpc, TempoCore.Empty_pb2
async def _set_actor_type_semantic_id(
    actor_type: str = '',
    semantic_id: int = 0
) -> TempoCore.Empty_pb2.Empty:
    stub = await tempo_context().get_stub(TempoSensors.Labels_pb2_grpc.LabelServiceStub)
    request = TempoSensors.Labels_pb2.SetActorTypeSemanticIdRequest(
        actor_type=actor_type,
        semantic_id=semantic_id
    )
    return await stub.SetActorTypeSemanticId(request)


def set_actor_type_semantic_id(
    actor_type: str = '',
    semantic_id: int = 0
) -> TempoCore.Empty_pb2.Empty:
    return run_async(_set_actor_type_semantic_id(
        actor_type=actor_type,
        semantic_id=semantic_id
    ))


@awaitable(set_actor_type_semantic_id)
async def set_actor_type_semantic_id(
    actor_type: str = '',
    semantic_id: int = 0
) -> TempoCore.Empty_pb2.Empty:
    return await _set_actor_type_semantic_id(
        actor_type=actor_type,
        semantic_id=semantic_id
    )

import TempoSensors.Labels_pb2, TempoSensors.Labels_pb2_grpc, TempoCore.Empty_pb2
async def _get_all_static_mesh_types(
) -> TempoSensors.Labels_pb2.GetAllStaticMeshTypesResponse:
    stub = await tempo_context().get_stub(TempoSensors.Labels_pb2_grpc.LabelServiceStub)
    request = TempoCore.Empty_pb2.Empty(
    )
    return await stub.GetAllStaticMeshTypes(request)


def get_all_static_mesh_types(
) -> TempoSensors.Labels_pb2.GetAllStaticMeshTypesResponse:
    return run_async(_get_all_static_mesh_types(
    ))


@awaitable(get_all_static_mesh_types)
async def get_all_static_mesh_types(
) -> TempoSensors.Labels_pb2.GetAllStaticMeshTypesResponse:
    return await _get_all_static_mesh_types(
    )

import TempoSensors.Labels_pb2, TempoSensors.Labels_pb2_grpc, TempoCore.Empty_pb2
async def _set_static_mesh_type_semantic_id(
    static_mesh_path: str = '',
    semantic_id: int = 0
) -> TempoCore.Empty_pb2.Empty:
    stub = await tempo_context().get_stub(TempoSensors.Labels_pb2_grpc.LabelServiceStub)
    request = TempoSensors.Labels_pb2.SetStaticMeshTypeSemanticIdRequest(
        static_mesh_path=static_mesh_path,
        semantic_id=semantic_id
    )
    return await stub.SetStaticMeshTypeSemanticId(request)


def set_static_mesh_type_semantic_id(
    static_mesh_path: str = '',
    semantic_id: int = 0
) -> TempoCore.Empty_pb2.Empty:
    return run_async(_set_static_mesh_type_semantic_id(
        static_mesh_path=static_mesh_path,
        semantic_id=semantic_id
    ))


@awaitable(set_static_mesh_type_semantic_id)
async def set_static_mesh_type_semantic_id(
    static_mesh_path: str = '',
    semantic_id: int = 0
) -> TempoCore.Empty_pb2.Empty:
    return await _set_static_mesh_type_semantic_id(
        static_mesh_path=static_mesh_path,
        semantic_id=semantic_id
    )

import TempoSensors.Sensors_pb2_grpc, TempoSensors.Sensors_pb2, TempoCore.Empty_pb2
async def _get_available_sensors(
) -> TempoSensors.Sensors_pb2.AvailableSensorsResponse:
    stub = await tempo_context().get_stub(TempoSensors.Sensors_pb2_grpc.SensorServiceStub)
    request = TempoCore.Empty_pb2.Empty(
    )
    return await stub.GetAvailableSensors(request)


def get_available_sensors(
) -> TempoSensors.Sensors_pb2.AvailableSensorsResponse:
    return run_async(_get_available_sensors(
    ))


@awaitable(get_available_sensors)
async def get_available_sensors(
) -> TempoSensors.Sensors_pb2.AvailableSensorsResponse:
    return await _get_available_sensors(
    )

import TempoSensors.Sensors_pb2_grpc, TempoSensors.Camera_pb2
async def _stream_color_images(
    owner: str = '',
    sensor: str = ''
) -> TempoSensors.Camera_pb2.ColorImage:
    stub = await tempo_context().get_stub(TempoSensors.Sensors_pb2_grpc.SensorServiceStub)
    request = TempoSensors.Camera_pb2.ColorImageRequest(
        owner=owner,
        sensor=sensor
    )
    async for response in stub.StreamColorImages(request):
        yield response


def stream_color_images(
    owner: str = '',
    sensor: str = ''
) -> TempoSensors.Camera_pb2.ColorImage:
    async_gen = _stream_color_images(
        owner=owner,
        sensor=sensor
    )
    while True:
        try:
            yield run_async(async_gen.__anext__())
        except StopAsyncIteration:
            break


@awaitable(stream_color_images)
async def stream_color_images(
    owner: str = '',
    sensor: str = ''
) -> TempoSensors.Camera_pb2.ColorImage:
    async for response in _stream_color_images(
        owner=owner,
        sensor=sensor
    ):
        yield response

import TempoSensors.Sensors_pb2_grpc, TempoSensors.Camera_pb2
async def _stream_depth_images(
    owner: str = '',
    sensor: str = ''
) -> TempoSensors.Camera_pb2.DepthImage:
    stub = await tempo_context().get_stub(TempoSensors.Sensors_pb2_grpc.SensorServiceStub)
    request = TempoSensors.Camera_pb2.DepthImageRequest(
        owner=owner,
        sensor=sensor
    )
    async for response in stub.StreamDepthImages(request):
        yield response


def stream_depth_images(
    owner: str = '',
    sensor: str = ''
) -> TempoSensors.Camera_pb2.DepthImage:
    async_gen = _stream_depth_images(
        owner=owner,
        sensor=sensor
    )
    while True:
        try:
            yield run_async(async_gen.__anext__())
        except StopAsyncIteration:
            break


@awaitable(stream_depth_images)
async def stream_depth_images(
    owner: str = '',
    sensor: str = ''
) -> TempoSensors.Camera_pb2.DepthImage:
    async for response in _stream_depth_images(
        owner=owner,
        sensor=sensor
    ):
        yield response

import TempoSensors.Sensors_pb2_grpc, TempoSensors.Camera_pb2
async def _stream_label_images(
    owner: str = '',
    sensor: str = ''
) -> TempoSensors.Camera_pb2.LabelImage:
    stub = await tempo_context().get_stub(TempoSensors.Sensors_pb2_grpc.SensorServiceStub)
    request = TempoSensors.Camera_pb2.LabelImageRequest(
        owner=owner,
        sensor=sensor
    )
    async for response in stub.StreamLabelImages(request):
        yield response


def stream_label_images(
    owner: str = '',
    sensor: str = ''
) -> TempoSensors.Camera_pb2.LabelImage:
    async_gen = _stream_label_images(
        owner=owner,
        sensor=sensor
    )
    while True:
        try:
            yield run_async(async_gen.__anext__())
        except StopAsyncIteration:
            break


@awaitable(stream_label_images)
async def stream_label_images(
    owner: str = '',
    sensor: str = ''
) -> TempoSensors.Camera_pb2.LabelImage:
    async for response in _stream_label_images(
        owner=owner,
        sensor=sensor
    ):
        yield response

import TempoSensors.Sensors_pb2_grpc, TempoSensors.Camera_pb2
async def _stream_bounding_boxes(
    owner: str = '',
    sensor: str = ''
) -> TempoSensors.Camera_pb2.BoundingBoxes:
    stub = await tempo_context().get_stub(TempoSensors.Sensors_pb2_grpc.SensorServiceStub)
    request = TempoSensors.Camera_pb2.BoundingBoxesRequest(
        owner=owner,
        sensor=sensor
    )
    async for response in stub.StreamBoundingBoxes(request):
        yield response


def stream_bounding_boxes(
    owner: str = '',
    sensor: str = ''
) -> TempoSensors.Camera_pb2.BoundingBoxes:
    async_gen = _stream_bounding_boxes(
        owner=owner,
        sensor=sensor
    )
    while True:
        try:
            yield run_async(async_gen.__anext__())
        except StopAsyncIteration:
            break


@awaitable(stream_bounding_boxes)
async def stream_bounding_boxes(
    owner: str = '',
    sensor: str = ''
) -> TempoSensors.Camera_pb2.BoundingBoxes:
    async for response in _stream_bounding_boxes(
        owner=owner,
        sensor=sensor
    ):
        yield response

import TempoSensors.Lidar_pb2, TempoSensors.Sensors_pb2_grpc
async def _stream_lidar_scans(
    owner: str = '',
    sensor: str = '',
    include_color: bool = False
) -> TempoSensors.Lidar_pb2.LidarScanSegment:
    stub = await tempo_context().get_stub(TempoSensors.Sensors_pb2_grpc.SensorServiceStub)
    request = TempoSensors.Lidar_pb2.LidarScanRequest(
        owner=owner,
        sensor=sensor,
        include_color=include_color
    )
    async for response in stub.StreamLidarScans(request):
        yield response


def stream_lidar_scans(
    owner: str = '',
    sensor: str = '',
    include_color: bool = False
) -> TempoSensors.Lidar_pb2.LidarScanSegment:
    async_gen = _stream_lidar_scans(
        owner=owner,
        sensor=sensor,
        include_color=include_color
    )
    while True:
        try:
            yield run_async(async_gen.__anext__())
        except StopAsyncIteration:
            break


@awaitable(stream_lidar_scans)
async def stream_lidar_scans(
    owner: str = '',
    sensor: str = '',
    include_color: bool = False
) -> TempoSensors.Lidar_pb2.LidarScanSegment:
    async for response in _stream_lidar_scans(
        owner=owner,
        sensor=sensor,
        include_color=include_color
    ):
        yield response

import TempoSensors.Sensors_pb2_grpc, TempoSensors.Camera_pb2
async def _stream_video(
    owner: str = '',
    sensor: str = '',
    codec: TempoSensors.Camera_pb2.VideoCodec = 0,
    bitrate_kbps: int = 0,
    keyframe_interval: int = 0,
    profile: TempoSensors.Camera_pb2.H264Profile = 0
) -> TempoSensors.Camera_pb2.VideoFrame:
    stub = await tempo_context().get_stub(TempoSensors.Sensors_pb2_grpc.SensorServiceStub)
    request = TempoSensors.Camera_pb2.VideoRequest(
        owner=owner,
        sensor=sensor,
        codec=codec,
        bitrate_kbps=bitrate_kbps,
        keyframe_interval=keyframe_interval,
        profile=profile
    )
    async for response in stub.StreamVideo(request):
        yield response


def stream_video(
    owner: str = '',
    sensor: str = '',
    codec: TempoSensors.Camera_pb2.VideoCodec = 0,
    bitrate_kbps: int = 0,
    keyframe_interval: int = 0,
    profile: TempoSensors.Camera_pb2.H264Profile = 0
) -> TempoSensors.Camera_pb2.VideoFrame:
    async_gen = _stream_video(
        owner=owner,
        sensor=sensor,
        codec=codec,
        bitrate_kbps=bitrate_kbps,
        keyframe_interval=keyframe_interval,
        profile=profile
    )
    while True:
        try:
            yield run_async(async_gen.__anext__())
        except StopAsyncIteration:
            break


@awaitable(stream_video)
async def stream_video(
    owner: str = '',
    sensor: str = '',
    codec: TempoSensors.Camera_pb2.VideoCodec = 0,
    bitrate_kbps: int = 0,
    keyframe_interval: int = 0,
    profile: TempoSensors.Camera_pb2.H264Profile = 0
) -> TempoSensors.Camera_pb2.VideoFrame:
    async for response in _stream_video(
        owner=owner,
        sensor=sensor,
        codec=codec,
        bitrate_kbps=bitrate_kbps,
        keyframe_interval=keyframe_interval,
        profile=profile
    ):
        yield response

