// Copyright Tempo Simulation, LLC. All Rights Reserved
//
// Auto-generated by gen_rust_api.py. Do not edit.

//! High-level API wrappers for TempoAgents.


use crate::streaming::SyncStreamIterator;


/// GetLanes
pub async fn get_lanes_async(
    tag_filter: crate::proto::tempo_agents::TagFilter,
    center: crate::proto::tempo_core::Vector2D,
    radius_m: f64,
) -> Result<crate::proto::tempo_agents::LaneDataResponse, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_agents::map_query_service_client::MapQueryServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_agents::LaneDataRequest {
        tag_filter: Some(tag_filter),
        center: Some(center),
        radius_m,
    };

    let response = client.get_lanes(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of get_lanes
pub fn get_lanes(
    tag_filter: crate::proto::tempo_agents::TagFilter,
    center: crate::proto::tempo_core::Vector2D,
    radius_m: f64,
) -> Result<crate::proto::tempo_agents::LaneDataResponse, crate::TempoError> {
    crate::context::RUNTIME.block_on(get_lanes_async(
        tag_filter,
        center,
        radius_m,
    ))
}

/// GetLaneAccessibility
pub async fn get_lane_accessibility_async(
    from_id: i32,
    to_id: i32,
) -> Result<crate::proto::tempo_agents::LaneAccessibilityResponse, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_agents::map_query_service_client::MapQueryServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_agents::LaneAccessibilityRequest {
        from_id,
        to_id,
    };

    let response = client.get_lane_accessibility(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of get_lane_accessibility
pub fn get_lane_accessibility(
    from_id: i32,
    to_id: i32,
) -> Result<crate::proto::tempo_agents::LaneAccessibilityResponse, crate::TempoError> {
    crate::context::RUNTIME.block_on(get_lane_accessibility_async(
        from_id,
        to_id,
    ))
}

/// StreamLaneAccessibility (server streaming)
pub async fn stream_lane_accessibility_async(
    from_id: i32,
    to_id: i32,
) -> Result<tonic::Streaming<crate::proto::tempo_agents::LaneAccessibilityResponse>, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_agents::map_query_service_client::MapQueryServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_agents::LaneAccessibilityRequest {
        from_id,
        to_id,
    };

    let response = client.stream_lane_accessibility(request).await?;
    Ok(response.into_inner())
}

/// Synchronous iterator version of stream_lane_accessibility
pub fn stream_lane_accessibility(
    from_id: i32,
    to_id: i32,
) -> Result<impl Iterator<Item = Result<crate::proto::tempo_agents::LaneAccessibilityResponse, tonic::Status>>, crate::TempoError> {
    let stream = crate::context::RUNTIME.block_on(stream_lane_accessibility_async(
        from_id,
        to_id,
    ))?;

    Ok(SyncStreamIterator::new(stream))
}

/// GetZones
pub async fn get_zones_async(
    tag_filter: crate::proto::tempo_agents::TagFilter,
    center: crate::proto::tempo_core::Vector,
    radius_m: f64,
) -> Result<crate::proto::tempo_agents::ZoneDataResponse, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_agents::map_query_service_client::MapQueryServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_agents::ZoneDataRequest {
        tag_filter: Some(tag_filter),
        center: Some(center),
        radius_m,
    };

    let response = client.get_zones(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of get_zones
pub fn get_zones(
    tag_filter: crate::proto::tempo_agents::TagFilter,
    center: crate::proto::tempo_core::Vector,
    radius_m: f64,
) -> Result<crate::proto::tempo_agents::ZoneDataResponse, crate::TempoError> {
    crate::context::RUNTIME.block_on(get_zones_async(
        tag_filter,
        center,
        radius_m,
    ))
}
