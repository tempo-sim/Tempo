// Copyright Tempo Simulation, LLC. All Rights Reserved
//
// Auto-generated by gen_rust_api.py. Do not edit.

//! High-level API wrappers for TempoSensors.


use crate::streaming::SyncStreamIterator;


/// GetInstanceToSemanticIdMap
pub async fn get_instance_to_semantic_id_map_async(
) -> Result<crate::proto::tempo_sensors::InstanceToSemanticIdMap, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_sensors::label_service_client::LabelServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_core::Empty {
    };

    let response = client.get_instance_to_semantic_id_map(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of get_instance_to_semantic_id_map
pub fn get_instance_to_semantic_id_map(
) -> Result<crate::proto::tempo_sensors::InstanceToSemanticIdMap, crate::TempoError> {
    crate::context::RUNTIME.block_on(get_instance_to_semantic_id_map_async(
    ))
}

/// GetAllActorLabels
pub async fn get_all_actor_labels_async(
) -> Result<crate::proto::tempo_sensors::GetAllActorLabelsResponse, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_sensors::label_service_client::LabelServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_core::Empty {
    };

    let response = client.get_all_actor_labels(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of get_all_actor_labels
pub fn get_all_actor_labels(
) -> Result<crate::proto::tempo_sensors::GetAllActorLabelsResponse, crate::TempoError> {
    crate::context::RUNTIME.block_on(get_all_actor_labels_async(
    ))
}

/// GetLabeledActorTypes
pub async fn get_labeled_actor_types_async(
) -> Result<crate::proto::tempo_sensors::GetLabeledActorTypesResponse, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_sensors::label_service_client::LabelServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_core::Empty {
    };

    let response = client.get_labeled_actor_types(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of get_labeled_actor_types
pub fn get_labeled_actor_types(
) -> Result<crate::proto::tempo_sensors::GetLabeledActorTypesResponse, crate::TempoError> {
    crate::context::RUNTIME.block_on(get_labeled_actor_types_async(
    ))
}

/// GetSemanticClasses
pub async fn get_semantic_classes_async(
) -> Result<crate::proto::tempo_sensors::GetSemanticClassesResponse, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_sensors::label_service_client::LabelServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_core::Empty {
    };

    let response = client.get_semantic_classes(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of get_semantic_classes
pub fn get_semantic_classes(
) -> Result<crate::proto::tempo_sensors::GetSemanticClassesResponse, crate::TempoError> {
    crate::context::RUNTIME.block_on(get_semantic_classes_async(
    ))
}

/// SetActorTypeSemanticId
pub async fn set_actor_type_semantic_id_async(
    actor_type: String,
    semantic_id: i32,
) -> Result<crate::proto::tempo_core::Empty, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_sensors::label_service_client::LabelServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_sensors::SetActorTypeSemanticIdRequest {
        actor_type,
        semantic_id,
    };

    let response = client.set_actor_type_semantic_id(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of set_actor_type_semantic_id
pub fn set_actor_type_semantic_id(
    actor_type: String,
    semantic_id: i32,
) -> Result<crate::proto::tempo_core::Empty, crate::TempoError> {
    crate::context::RUNTIME.block_on(set_actor_type_semantic_id_async(
        actor_type,
        semantic_id,
    ))
}

/// GetAllStaticMeshTypes
pub async fn get_all_static_mesh_types_async(
) -> Result<crate::proto::tempo_sensors::GetAllStaticMeshTypesResponse, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_sensors::label_service_client::LabelServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_core::Empty {
    };

    let response = client.get_all_static_mesh_types(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of get_all_static_mesh_types
pub fn get_all_static_mesh_types(
) -> Result<crate::proto::tempo_sensors::GetAllStaticMeshTypesResponse, crate::TempoError> {
    crate::context::RUNTIME.block_on(get_all_static_mesh_types_async(
    ))
}

/// SetStaticMeshTypeSemanticId
pub async fn set_static_mesh_type_semantic_id_async(
    static_mesh_path: String,
    semantic_id: i32,
) -> Result<crate::proto::tempo_core::Empty, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_sensors::label_service_client::LabelServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_sensors::SetStaticMeshTypeSemanticIdRequest {
        static_mesh_path,
        semantic_id,
    };

    let response = client.set_static_mesh_type_semantic_id(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of set_static_mesh_type_semantic_id
pub fn set_static_mesh_type_semantic_id(
    static_mesh_path: String,
    semantic_id: i32,
) -> Result<crate::proto::tempo_core::Empty, crate::TempoError> {
    crate::context::RUNTIME.block_on(set_static_mesh_type_semantic_id_async(
        static_mesh_path,
        semantic_id,
    ))
}

/// GetAvailableSensors
pub async fn get_available_sensors_async(
) -> Result<crate::proto::tempo_sensors::AvailableSensorsResponse, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_sensors::sensor_service_client::SensorServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_core::Empty {
    };

    let response = client.get_available_sensors(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of get_available_sensors
pub fn get_available_sensors(
) -> Result<crate::proto::tempo_sensors::AvailableSensorsResponse, crate::TempoError> {
    crate::context::RUNTIME.block_on(get_available_sensors_async(
    ))
}

/// StreamColorImages (server streaming)
pub async fn stream_color_images_async(
    owner: String,
    sensor: String,
) -> Result<tonic::Streaming<crate::proto::tempo_sensors::ColorImage>, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_sensors::sensor_service_client::SensorServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_sensors::ColorImageRequest {
        owner,
        sensor,
    };

    let response = client.stream_color_images(request).await?;
    Ok(response.into_inner())
}

/// Synchronous iterator version of stream_color_images
pub fn stream_color_images(
    owner: String,
    sensor: String,
) -> Result<impl Iterator<Item = Result<crate::proto::tempo_sensors::ColorImage, tonic::Status>>, crate::TempoError> {
    let stream = crate::context::RUNTIME.block_on(stream_color_images_async(
        owner,
        sensor,
    ))?;

    Ok(SyncStreamIterator::new(stream))
}

/// StreamDepthImages (server streaming)
pub async fn stream_depth_images_async(
    owner: String,
    sensor: String,
) -> Result<tonic::Streaming<crate::proto::tempo_sensors::DepthImage>, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_sensors::sensor_service_client::SensorServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_sensors::DepthImageRequest {
        owner,
        sensor,
    };

    let response = client.stream_depth_images(request).await?;
    Ok(response.into_inner())
}

/// Synchronous iterator version of stream_depth_images
pub fn stream_depth_images(
    owner: String,
    sensor: String,
) -> Result<impl Iterator<Item = Result<crate::proto::tempo_sensors::DepthImage, tonic::Status>>, crate::TempoError> {
    let stream = crate::context::RUNTIME.block_on(stream_depth_images_async(
        owner,
        sensor,
    ))?;

    Ok(SyncStreamIterator::new(stream))
}

/// StreamLabelImages (server streaming)
pub async fn stream_label_images_async(
    owner: String,
    sensor: String,
) -> Result<tonic::Streaming<crate::proto::tempo_sensors::LabelImage>, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_sensors::sensor_service_client::SensorServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_sensors::LabelImageRequest {
        owner,
        sensor,
    };

    let response = client.stream_label_images(request).await?;
    Ok(response.into_inner())
}

/// Synchronous iterator version of stream_label_images
pub fn stream_label_images(
    owner: String,
    sensor: String,
) -> Result<impl Iterator<Item = Result<crate::proto::tempo_sensors::LabelImage, tonic::Status>>, crate::TempoError> {
    let stream = crate::context::RUNTIME.block_on(stream_label_images_async(
        owner,
        sensor,
    ))?;

    Ok(SyncStreamIterator::new(stream))
}

/// StreamBoundingBoxes (server streaming)
pub async fn stream_bounding_boxes_async(
    owner: String,
    sensor: String,
) -> Result<tonic::Streaming<crate::proto::tempo_sensors::BoundingBoxes>, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_sensors::sensor_service_client::SensorServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_sensors::BoundingBoxesRequest {
        owner,
        sensor,
    };

    let response = client.stream_bounding_boxes(request).await?;
    Ok(response.into_inner())
}

/// Synchronous iterator version of stream_bounding_boxes
pub fn stream_bounding_boxes(
    owner: String,
    sensor: String,
) -> Result<impl Iterator<Item = Result<crate::proto::tempo_sensors::BoundingBoxes, tonic::Status>>, crate::TempoError> {
    let stream = crate::context::RUNTIME.block_on(stream_bounding_boxes_async(
        owner,
        sensor,
    ))?;

    Ok(SyncStreamIterator::new(stream))
}

/// StreamLidarScans (server streaming)
pub async fn stream_lidar_scans_async(
    owner: String,
    sensor: String,
    include_color: bool,
) -> Result<tonic::Streaming<crate::proto::tempo_sensors::LidarScanSegment>, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_sensors::sensor_service_client::SensorServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_sensors::LidarScanRequest {
        owner,
        sensor,
        include_color,
    };

    let response = client.stream_lidar_scans(request).await?;
    Ok(response.into_inner())
}

/// Synchronous iterator version of stream_lidar_scans
pub fn stream_lidar_scans(
    owner: String,
    sensor: String,
    include_color: bool,
) -> Result<impl Iterator<Item = Result<crate::proto::tempo_sensors::LidarScanSegment, tonic::Status>>, crate::TempoError> {
    let stream = crate::context::RUNTIME.block_on(stream_lidar_scans_async(
        owner,
        sensor,
        include_color,
    ))?;

    Ok(SyncStreamIterator::new(stream))
}

/// StreamVideo (server streaming)
pub async fn stream_video_async(
    owner: String,
    sensor: String,
    codec: i32,
    bitrate_kbps: u32,
    keyframe_interval: u32,
    profile: i32,
) -> Result<tonic::Streaming<crate::proto::tempo_sensors::VideoFrame>, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_sensors::sensor_service_client::SensorServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_sensors::VideoRequest {
        owner,
        sensor,
        codec,
        bitrate_kbps,
        keyframe_interval,
        profile,
    };

    let response = client.stream_video(request).await?;
    Ok(response.into_inner())
}

/// Synchronous iterator version of stream_video
pub fn stream_video(
    owner: String,
    sensor: String,
    codec: i32,
    bitrate_kbps: u32,
    keyframe_interval: u32,
    profile: i32,
) -> Result<impl Iterator<Item = Result<crate::proto::tempo_sensors::VideoFrame, tonic::Status>>, crate::TempoError> {
    let stream = crate::context::RUNTIME.block_on(stream_video_async(
        owner,
        sensor,
        codec,
        bitrate_kbps,
        keyframe_interval,
        profile,
    ))?;

    Ok(SyncStreamIterator::new(stream))
}
