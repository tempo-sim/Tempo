// Copyright Tempo Simulation, LLC. All Rights Reserved
//
// Auto-generated by gen_rust_api.py. Do not edit.

//! High-level API wrappers for TempoGeographic.



/// SetDate
pub async fn set_date_async(
    day: u32,
    month: u32,
    year: u32,
) -> Result<crate::proto::tempo_core::Empty, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_geographic::geographic_service_client::GeographicServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_geographic::Date {
        day,
        month,
        year,
    };

    let response = client.set_date(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of set_date
pub fn set_date(
    day: u32,
    month: u32,
    year: u32,
) -> Result<crate::proto::tempo_core::Empty, crate::TempoError> {
    crate::context::RUNTIME.block_on(set_date_async(
        day,
        month,
        year,
    ))
}

/// SetTimeOfDay
pub async fn set_time_of_day_async(
    hour: u32,
    minute: u32,
    second: u32,
) -> Result<crate::proto::tempo_core::Empty, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_geographic::geographic_service_client::GeographicServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_geographic::TimeOfDay {
        hour,
        minute,
        second,
    };

    let response = client.set_time_of_day(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of set_time_of_day
pub fn set_time_of_day(
    hour: u32,
    minute: u32,
    second: u32,
) -> Result<crate::proto::tempo_core::Empty, crate::TempoError> {
    crate::context::RUNTIME.block_on(set_time_of_day_async(
        hour,
        minute,
        second,
    ))
}

/// SetDayCycleRelativeRate
pub async fn set_day_cycle_relative_rate_async(
    rate: f32,
) -> Result<crate::proto::tempo_core::Empty, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_geographic::geographic_service_client::GeographicServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_geographic::DayCycleRateRequest {
        rate,
    };

    let response = client.set_day_cycle_relative_rate(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of set_day_cycle_relative_rate
pub fn set_day_cycle_relative_rate(
    rate: f32,
) -> Result<crate::proto::tempo_core::Empty, crate::TempoError> {
    crate::context::RUNTIME.block_on(set_day_cycle_relative_rate_async(
        rate,
    ))
}

/// GetDateTime
pub async fn get_date_time_async(
) -> Result<crate::proto::tempo_geographic::DateTime, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_geographic::geographic_service_client::GeographicServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_core::Empty {
    };

    let response = client.get_date_time(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of get_date_time
pub fn get_date_time(
) -> Result<crate::proto::tempo_geographic::DateTime, crate::TempoError> {
    crate::context::RUNTIME.block_on(get_date_time_async(
    ))
}

/// SetGeographicReference
pub async fn set_geographic_reference_async(
    origin: crate::proto::tempo_geographic::GeographicCoordinate,
    rotation: crate::proto::tempo_core::Rotation,
) -> Result<crate::proto::tempo_core::Empty, crate::TempoError> {
    let channel = crate::context::connected_channel().await?;
    let mut client = crate::proto::tempo_geographic::geographic_service_client::GeographicServiceClient::new(channel)
        .max_decoding_message_size(crate::context::MAX_MESSAGE_SIZE)
        .max_encoding_message_size(crate::context::MAX_MESSAGE_SIZE);

    let request = crate::proto::tempo_geographic::GeographicReference {
        origin: Some(origin),
        rotation: Some(rotation),
    };

    let response = client.set_geographic_reference(request).await?;
    Ok(response.into_inner())
}

/// Synchronous version of set_geographic_reference
pub fn set_geographic_reference(
    origin: crate::proto::tempo_geographic::GeographicCoordinate,
    rotation: crate::proto::tempo_core::Rotation,
) -> Result<crate::proto::tempo_core::Empty, crate::TempoError> {
    crate::context::RUNTIME.block_on(set_geographic_reference_async(
        origin,
        rotation,
    ))
}
